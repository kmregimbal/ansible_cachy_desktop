- name: Play to set up host for full ansible playbook
  hosts: localhost
  connection: local
  #become: true

  tasks:
    - name: Install requirements
      ansible.builtin.command: ansible-galaxy install -r requirements.yml
